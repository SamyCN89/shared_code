# shared_code/setup.py
from setuptools import setup, find_packages

setup(
    name="shared_code",
    version="0.1.0",
    packages=find_packages(),
    author="SamyCN89",
    description="Reusable functions for functional connectivity and analysis",
)
